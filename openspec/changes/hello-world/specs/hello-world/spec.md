# Spec: Hello World

## Externally Observable Behavior
- When executed, the program SHALL print the string "Hello, World!" (including the trailing newline) to standard output.
- The program SHALL exit with status code 0.