# 卡点音频方案 audio_prompt.md

> 基于 brief.md §4.2 与已渲染场景 `src/scenes/SceneBark.tsx` 的 `BARK_PULSES` 帧网格换算。
> 用途：供人工用 AI 音效/音乐工具生成 → 试听 → 剪映拼接 → 回填成片 `out/video_final.mp4`。

---

## 0. 一句话目标

为 3.0s 竖屏「小狗叫」视频产出**卡点同步音效方案**：3 声真实幼犬短促叫声，峰值对齐嘴部张开峰值帧；无 BGM、无字幕、硬切收尾。

---

## 1. 硬性约束（来自 brief.md，不得覆盖）

| 约束 | 取值 | 来源 |
|---|---|---|
| 音频类型 | **真实幼犬叫声 SFX + 极轻室内底噪**；**无 BGM、无音乐** | brief §4.2 / §5 |
| 叫声规格 | 短促 2–3 声，落在 0.5–2.5s 区间 | brief §4.2 / D9 |
| 排除项 | 不得用成年犬低吼、合成拟音、公园/室外环境音 | brief §4.2 |
| 同步容差 | **≤ 1 帧 = 33.33ms** | brief §7 / verified_data D4 |
| 总时长 | 3.000s，无淡入淡出、无留白、片尾硬切 | brief §1 / §6.1 / D1 |
| 嘴部开合与叫声 | 同帧同步（onset→peak→offset 三点对齐） | brief §4.2 / §7 |

> ⚠️ 关于「卡点音乐」：Brief 硬性规定**无 BGM**，因此本方案交付的是**叫声 SFX + 底噪**，而非配乐。
> 若后续需要加音乐，必须先显式覆盖 brief.md §4.2，否则违反验收标准 §6.4。本方案不产出 BGM 提示词。

---

## 2. 视频节奏与卡点时间轴（核心）

### 2.1 场景分段

| 段 | 场景 | 时间段 | 全局帧 | 帧数 | 声音内容 |
|---|---|---|---|---|---|
| S1 | 入画定位 | 0.000–0.500s | 0–14 | 15 | 极轻室内底噪，未出声 |
| S2 | 张嘴叫 | 0.500–2.500s | 15–74 | 60 | 3 声短促幼犬叫（卡点） |
| S3 | 收尾定格 | 2.500–3.000s | 75–89 | 15 | 第 3 声余音/混响尾收，自然结束 |

### 2.2 三声叫声卡点（由 `BARK_PULSES` 换算，fps=30）

> `BARK_PULSES` 为 S2 内局部帧；全局帧 = 局部帧 + 15（S2 起始帧）。
> 时间 = 全局帧 / 30。单帧 = 33.333ms。

| 叫声 | 事件 | 局部帧 | 全局帧 | 时间(s) | 时间(ms) | 含义 |
|---|---|---|---|---|---|---|
| #1 | onset | 4 | 19 | 0.6333 | 633 | 嘴开始张 / 叫声起音 |
| #1 | **peak** | 8 | 23 | **0.7667** | **767** | 嘴全张 / 叫声峰值（最响） |
| #1 | offset | 13 | 28 | 0.9333 | 933 | 嘴闭合 / 叫声收 |
| #2 | onset | 24 | 39 | 1.3000 | 1300 | 起音 |
| #2 | **peak** | 28 | 43 | **1.4333** | **1433** | 峰值 |
| #2 | offset | 33 | 48 | 1.6000 | 1600 | 收 |
| #3 | onset | 44 | 59 | 1.9667 | 1967 | 起音 |
| #3 | **peak** | 48 | 63 | **2.1000** | **2100** | 峰值 |
| #3 | offset | 53 | 68 | 2.2667 | 2267 | 收 |

**三声峰值（最响点）卡点 = 767ms / 1433ms / 2100ms。**
峰值间隔 = 666ms、667ms（≈ 20 帧 ≈ 0.667s，等距节奏）。
单声持续 onset→offset = 9 帧 = 300ms（其中可感知叫声约 250–300ms，峰值居中偏后）。

### 2.3 S3 余音尾（2.500–3.000s）

`SceneFreeze.tsx` 嘴部开度 0.18→0.07（帧 0–6，即 2.500–2.700s）→ 0.05 保持至 3.000s。
对应音频：第 3 声叫完后保留短混响/自然余音，约 2.500–2.800s 衰减，**不得淡出**，3.000s 硬切。

---

## 3. SFX 生成提示词（供人工投喂 AI 音效工具）

> 目标工具建议：ElevenLabs SFX / Stable Audio / Pika 音效 / 通用 AI SFX 生成器。
> 优先选「真实录音素材库幼犬叫声」二次剪辑，AI 生成仅作补充。每个单声单独生成，便于剪映对齐。

### 3.1 单声幼犬叫（生成 3 次，或一条含 3 声等距脉冲）

**中文提示词：**
```
真实中华田园犬幼崽（约2-3月龄）的短促叫声，单声"汪"，清亮略带奶音，非成年犬低吼。
单声时长约250-300毫秒，起音急促、峰值居中、尾音快速收束，不拖长。
室内近场录音质感，干燥无混响、无背景音乐、无室外环境音、无人声。
单声道，干净，未加效果。
```

**English prompt（适配英文工具）：**
```
Real puppy bark, single short "yip", from a 2-3 month old Chinese rural dog puppy.
Bright, slightly squeaky, young and cute — NOT an adult dog growl or low bark.
Duration about 250-300ms: fast attack, peak near the middle, quick clean decay, no long tail.
Dry indoor close-mic recording, no reverb, no background music, no outdoor ambience, no human voice.
Mono, clean, unprocessed.
```

**参数建议（工具支持时）：**
- 时长：0.30s（单声）；如生成整条则 2.30s（覆盖 0.633–2.267s）
- 风格/标签：`puppy, yip, short bark, indoor, dry, cute`
- 负面提示：`adult dog, growl, synth, music, ambience, reverb, echo, human`

### 3.2 室内底噪（S1 0.0–0.5s 极轻）

```
极安静的室内房间底噪，近无声，仅极微弱的空气声/远处不可辨低频。
无音乐、无人声、无室外交通/鸟鸣/风声。
电平极低，约 -45 ~ -50 dBFS，仅作存在感铺垫。
时长 0.5 秒。
```
English: `Very quiet indoor room tone, almost silent, faint air presence, no music/voice/outdoor sound, ~0.5s, very low level.`

> 注：底噪为可选项。Brief §4.2 仅在 S1 提「极轻环境底噪」；若叫声足够干净，可直接留静音，避免底噪抢戏。

---

## 4. 剪映（JianYing）拼接方案

### 4.1 素材准备
1. 用 AI 工具生成 / 素材库选取 **3 声幼犬叫**（建议每声单独导出为 wav/mp3，便于精细对齐）。
2. （可选）生成 0.5s 室内底噪。
3. 渲染好的无声视频：Remotion 导出 `Main` composition（1080×1920, 30fps, 3.0s, 无音频轨）。

### 4.2 工程设置
- 画布：1080×1920 竖屏 9:16，帧率 **30fps**。
- 导入无声视频放主轨，时间 0–3.000s。
- 关闭剪映自动「音频淡入淡出」「智能配乐」，避免违反 Brief 无淡入淡出约束。

### 4.3 叫声对齐（按峰值卡点）
将 3 个叫声音轨片段的**最响点**对齐到下列时间码（剪映时间轴，毫秒）：

| 叫声 | 片段峰值对齐时间 | 片段建议区间（onset→offset） |
|---|---|---|
| #1 | **0.767s** | 0.633s – 0.933s |
| #2 | **1.433s** | 1.300s – 1.600s |
| #3 | **2.100s** | 1.967s – 2.267s |

- 在剪映用「逐帧」预览（←/→ 键）微调，使叫声峰值与画面嘴部全张帧同帧。
- 容差 **≤ 33ms（1 帧）**；超过 1 帧即判定声画不同步，需重对齐。
- 第 3 声保留自然尾音进入 S3（2.500–~2.800s），**不加淡出**，3.000s 由视频硬切结束。

### 4.4 （可选）底噪铺设
- 底噪片段放 0.000–0.500s，电平极低（-45dBFS 以下），0.5s 后切除或保持极低，不可盖过叫声。

### 4.5 导出
- 格式：MP4，视频 H.264，音频 AAC。
- 分辨率 1080×1920，30fps，时长精确 3.000s。
- 输出路径：**`out/video_final.mp4`**（回填至本仓库）。

---

## 5. 验收自检（对照 brief §6）

| # | 验收项 | 检查方法 |
|---|---|---|
| 1 | 时长精确 3.0s，无淡入淡出、无留白 | 剪映/ffprobe 确认总时长 = 3.000s，首尾无淡变 |
| 2 | 1080×1920, MP4(H.264+AAC), 30fps | ffprobe 确认编码/分辨率/帧率 |
| 3 | 声画同步（嘴部开合与叫声同帧） | 逐帧预览：峰值帧 0.767/1.433/2.100s 处嘴全张且叫声最响 |
| 4 | 真实幼犬叫声，无 BGM，无字幕/水印/Logo | 听感确认无配乐；画面无文字/角标 |
| 5 | 卡点容差 ≤ 1 帧（33ms） | 峰值对齐误差逐帧核对 |

### ffprobe 一键自检命令
```bash
ffprobe -v error -select_streams v:0 -show_entries stream=width,height,r_frame_rate,codec_name \
  -select_streams a:0 -show_entries stream=codec_name,sample_rate \
  -show_entries format=duration -of default=noprint_wrappers=1 out/video_final.mp4
```
预期：width=1080 height=1920 r_frame_rate=30/1 codec_name=h264；音频 codec_name=aac；duration=3.000000。

---

## 6. 回填目标

- 最终成片：**`out/video_final.mp4`**
- 回填后该文件即为本任务交付物，供下游验收。
- 若人工多次试听迭代，请以最后一次覆盖 `out/video_final.mp4` 为准。
