# 需求澄清与设计方案

## 概述

基于需求"分别写三个接口helloworld、哈希算法以及冒泡排序；前端新增页面展示；导出功能；埋点统计与可视化报表"进行需求澄清与设计。

**确认技术栈**: Python Flask + 原生HTML/JS/CSS

---

## 一、需求澄清结果

### 已确认项
| 条目 | 确认结果 |
|------|---------|
| 技术栈 | Python Flask（后端）+ 原生HTML/JS/CSS（前端） |
| 后端接口数 | 3个：helloworld、哈希算法（SHA256 + MD5 双算法）、冒泡排序 |
| 仓库分配 | `manyu_test` = 后端仓库，`manyu_test1` = 前端仓库 |
| 前端页面 | 单页面，3个Tab分别展示各接口执行结果 |
| 导出功能 | 前端导出按钮 + 后端导出接口 |
| 埋点统计 | 记录调用次数和调用人 |
| 可视化报表 | 折线图、饼图、柱状图，维度：人员类型、人员层级、人员部门 |

### 默认设计假设（待后续确认）

> **说明**: 以下为基于已确认技术栈的合理默认设计，可在后续实施阶段调整。

| 决策项 | 默认方案 | 备选方案 |
|--------|---------|---------|
| 前端可视化库 | ECharts（CDN 引入） | Chart.js |
| 导出格式 | CSV | Excel / PDF |
| 调用人识别 | 页面输入用户名（简单方案） | 登录认证系统 |
| 冒泡排序复用 | 复用已有 `bubble_sort.py`，包装为 Flask 接口 | |

---

## 二、系统架构设计

### 整体架构

```
┌─────────────────────────────────────────────────┐
│                  浏览器 (前端)                    │
│  ┌──────────┐  ┌──────────┐  ┌───────────────┐  │
│  │ Tab 页面  │  │ 导出按钮  │  │ 报表可视化页面 │  │
│  │ (3个Tab)  │  │          │  │ (折线/饼/柱状图)│  │
│  └─────┬─────┘  └────┬─────┘  └───────┬───────┘  │
└────────┼──────────────┼────────────────┼──────────┘
         │              │                │
         ▼              ▼                ▼
┌─────────────────────────────────────────────────┐
│              Flask 后端 (manyu_test)              │
│  ┌──────────┐  ┌──────────┐  ┌───────────────┐  │
│  │ API 接口  │  │ 导出接口  │  │ 埋点统计接口   │  │
│  │ /api/*   │  │ /api/export│  │ /api/stats/*  │  │
│  └─────┬─────┘  └────┬─────┘  └───────┬───────┘  │
│        │             │                │          │
│        ▼             ▼                ▼          │
│  ┌──────────┐  ┌──────────┐  ┌───────────────┐  │
│  │ 算法模块  │  │ 导出模块  │  │ 埋点数据存储   │  │
│  │ (3个接口) │  │          │  │ (内存/文件/DB) │  │
│  └──────────┘  └──────────┘  └───────────────┘  │
└─────────────────────────────────────────────────┘
```

### 模块划分

#### 后端模块（manyu_test）

| 模块 | 文件 | 职责 |
|------|------|------|
| API 路由 | `app.py` | Flask 应用入口，注册所有路由 |
| HelloWorld | `hello_world.py` | HelloWorld 接口实现 |
| 哈希算法 | `hash_algo.py` | 哈希算法接口实现 |
| 冒泡排序 | `bubble_sort.py` | 已有文件，包装为接口 |
| 埋点模块 | `tracking.py` | 调用次数、调用人记录的埋点逻辑 |
| 统计模块 | `stats.py` | 统计数据聚合与查询 |
| 导出模块 | `export.py` | 导出接口实现 |

#### 前端模块（manyu_test1）

| 文件 | 职责 |
|------|------|
| `index.html` | 主页面，包含3个Tab + 导出按钮 + 报表区域 |
| `style.css` | 样式文件 |
| `app.js` | 前端逻辑，API调用、Tab切换、图表渲染 |

---

## 三、接口设计

### 3.1 业务接口

#### GET /api/hello
- 描述: HelloWorld 接口
- 响应: `{ "message": "Hello World!", "timestamp": "..." }`

#### POST /api/hash
- 描述: 哈希算法接口
- 请求体: `{ "input": "待哈希字符串", "algorithm": "sha256" }`
- 响应: `{ "input": "...", "algorithm": "sha256", "hash": "..." }`

#### POST /api/sort
- 描述: 冒泡排序接口
- 请求体: `{ "data": [5, 3, 8, 4, 2] }`
- 响应: `{ "original": [...], "sorted": [...], "algorithm": "bubble_sort" }`

### 3.2 导出接口

#### GET /api/export?tab=hello&format=csv
- 描述: 导出指定Tab页面的结果
- 参数: `tab` (hello/hash/sort), `format` (csv)
- 响应: CSV 文件下载

### 3.3 埋点统计接口

#### POST /api/track
- 描述: 记录调用（由后端内部自动调用，也可前端主动上报）
- 请求体: `{ "action": "call_api", "api": "hello", "caller": "username", "user_type": "developer", "user_level": "senior", "department": "engineering" }`

#### GET /api/stats/overview
- 描述: 获取统计数据概览
- 响应: `{ "total_calls": 100, "by_api": {...}, "by_user": {...} }`

#### GET /api/stats/chart?dimension=user_type&chart_type=pie
- 描述: 按维度获取图表数据
- 参数: `dimension` (user_type/user_level/department), `chart_type` (pie/line/bar)
- 响应: 图表渲染所需的数据结构

---

## 四、数据模型

### 埋点记录

```json
{
  "id": "uuid",
  "timestamp": "2026-09-01T12:00:00Z",
  "api": "hello|hash|sort",
  "action": "call_api|export|view_report",
  "caller": "用户名",
  "user_type": "developer|manager|tester|admin",
  "user_level": "junior|mid|senior|principal",
  "department": "engineering|product|qa|operations"
}
```

### 统计维度

| 维度 | 字段 | 描述 |
|------|------|------|
| 人员类型 | `user_type` | developer / manager / tester / admin |
| 人员层级 | `user_level` | junior / mid / senior / principal |
| 人员部门 | `department` | engineering / product / qa / operations |

---

## 五、前端页面设计

### 页面布局

```
┌──────────────────────────────────────────────────────┐
│  [Tab: HelloWorld]  [Tab: 哈希算法]  [Tab: 冒泡排序]  │ [导出按钮]
├──────────────────────────────────────────────────────┤
│                                                       │
│               Tab 内容区域                             │
│         （输入参数 → 调用接口 → 展示结果）              │
│                                                       │
├──────────────────────────────────────────────────────┤
│  📊 调用统计报表                                       │
│  ┌───────────┬───────────┬───────────┐               │
│  │ 折线图     │  饼图     │  柱状图    │               │
│  │ (趋势)    │ (人员类型) │ (人员层级) │               │
│  └───────────┴───────────┴───────────┘               │
│  维度切换: [人员类型] [人员层级] [人员部门]              │
└──────────────────────────────────────────────────────┘
```

### Tab 功能说明

| Tab | 输入 | 操作 | 展示 |
|-----|------|------|------|
| HelloWorld | 无 | 调用 GET /api/hello | 显示返回消息和时间戳 |
| 哈希算法 | 文本输入框 + 算法选择 | 调用 POST /api/hash | 显示原文本和哈希值 |
| 冒泡排序 | 数字数组输入 | 调用 POST /api/sort | 显示排序前后的数组 |

---

## 六、待澄清问题（后续节点处理）

以下问题在当前阶段未获得明确答复，建议在实施阶段逐个确认：

1. **仓库分配**: `manyu_test` 做后端、`manyu_test1` 做前端，还是统一放一个仓？
2. **哈希算法类型**: SHA256 / MD5 / 自定义？
3. **导出格式**: CSV / Excel / PDF？
4. **调用人识别**: 页面输入用户名 / 模拟数据？
5. **存储方式**: 内存存储 / 文件存储 / SQLite？

---

## 七、实施计划（概要）

| 阶段 | 内容 | 预计工作量 |
|------|------|-----------|
| 1. 后端接口 | Flask 应用 + 3个接口 + 冒泡排序接口包装 | 小 |
| 2. 埋点模块 | 埋点记录 + 统计聚合 + 查询接口 | 中 |
| 3. 导出接口 | CSV 导出功能 | 小 |
| 4. 前端页面 | index.html + 3个Tab + 接口调用 | 中 |
| 5. 可视化报表 | ECharts 集成 + 3种图表 + 维度切换 | 中 |
| 6. 联调测试 | 前后端联调 | 小 |